services:
  postgres:
    image: postgres:16-alpine
    container_name: shareit-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: shareit
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5432:5432"
    volumes:
      - shareit-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test -d shareit"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  shareit-postgres-data:
